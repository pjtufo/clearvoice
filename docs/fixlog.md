# ClearVoice Fixlog（修复与变更记录）

格式：`[日期] 类别 | 问题 → 原因 → 修复`。每轮修改后均以 `smoke_test.py` 全量回归验证。

---

## 2026-08-31 首轮开发（基础功能）

### [2026-08-31] 环境 | uv 不在 PATH

- **问题**：`uv --version` 报「不是内部或外部命令」。
- **原因**：uv 未安装，PowerShell 会话 PATH 不含用户安装目录。
- **修复**：官方安装脚本安装 uv（`--user` 级别），实际落盘 `C:\Users\FXAQ_PJT\.local\bin\uv.exe`；
  调用时使用绝对路径。

### [2026-08-31] 环境 | uv 安装权限被拒

- **问题**：uv 安装脚本写系统目录失败。
- **修复**：以 `--user` 方式安装到用户目录。

### [2026-08-31] 环境 | PowerShell 执行策略阻止脚本

- **问题**：执行任意 `.ps1` 报「running scripts is disabled on this system」。
- **原因**：系统执行策略为 Restricted。
- **修复**：工具链调用改用 `Bypass`；注意该策略只影响 profile/包装脚本加载（无害告警），
  程序自身与 ffmpeg 调用不受影响。

### [2026-08-31] 环境 | 依赖安装网络失败

- **问题**：uv 从默认源拉取 PySide6/numpy 等超时。
- **修复**：切换清华镜像 `UV_DEFAULT_INDEX=https://pypi.tuna.tsinghua.edu.cn/simple`。

### [2026-08-31] 算法 | 说话人相似度匹配失效

- **问题**：同一个人前后说话，MFCC 相似度曲线无峰，消除命中 0 段。
- **原因**：MFCC 对通道/录音条件敏感，参考片段与目标片段均值不统一。
- **修复**：特征改为 **log-mel 谱 + CMN 归一化**（整段频点均值扣除，参考投影到同一空间）；
  `features.similarity_curve` 重写为该方案。

### [2026-08-31] 算法 | 余弦相似度数值偏低

- **问题**：同人相似度仅 0.3~0.5，默认阈值下漏检。
- **原因**：参考片段过短时窗均值退化；静音窗拉低分数。
- **修复**：
  - 参考窗长自适应：参考 < 1.5s 时按 `ref_dur/4` 缩短窗长（下限 0.12s）；
  - 参考端仅取能量前 60% 的窗参与均值；
  - 目标端能量低于峰值 6dB 的静音帧相似度置 -1；
  - 阈值整定：说话人消除 0.70、相似特征剔除 0.78、特征分割 0.80（界面可调）。

### [2026-08-31] 测试 | GUI 无法在无显示环境验证

- **修复**：冒烟测试使用 `QT_QPA_PLATFORM=offscreen` 构造 MainWindow，CI/脚本环境可跑。

---

## 2026-09-01 语音识别（转写与导出）

### [2026-09-01] 功能 | ASR 依赖缺失

- **问题**：文字特征剔除/识别功能无依赖可用（venv 中无 funasr/modelscope/torch）。
- **修复**：`uv sync --extra modelscope`（清华镜像）安装 funasr + modelscope + torch 2.13 + torchaudio，
  耗时约 20 分钟（torch 体积大），安装完成 PySide6 6.9.3 等原依赖未被破坏。

### [2026-09-01] 变更 | 导出任务完成时误弹「加载到播放器」

- **问题**：识别导出返回 dict 含 `output` 时会对 txt 文件弹播放器加载确认。
- **修复**：识别导出返回值只含 `report`（不含 `output`），`_default_done` 不再询问加载。

### [2026-09-01] 健壮性 | 临时 wav 清理

- **问题**：识别中途抛异常时 `src.asr.wav` 临时文件残留。
- **修复**：`try/finally` 中删除临时文件（`_job_asr_export`、`_job_split_keyword` 等）。

---

## 2026-09-01 关键词/正则分割与设置页

### [2026-09-01] 变更 | ASR 模型 ID 与本地模型不匹配

- **问题**：原配置 `iic/speech_paraformer-large_asr_nat-…` 在用户本地 `D:\funasrModel` 不存在，
  按原逻辑会触发在线下载（约 1GB）。
- **原因**：本地已有的是 **SeACo-Paraformer** 版本（`speech_seaco_paraformer_large_asr_nat-…`）。
- **修复**：`ASR_MODEL_ID` 更换为 SeACo 模型；新增 `_local()` 本地解析，
  按 `<根>/<模型名>`、`<根>/hub/iic/<模型名>`、`<根>/iic/<模型名>` 探测 `config.yaml`，
  命中即完全离线加载。已验证 ASR/VAD/标点三个模型全部命中本地目录。

### [2026-09-01] 设计 | 关键词匹配受 ASR 标点干扰

- **问题**：接入标点模型后，关键词「大家好」可能因中间标点/多字 token 匹配失败或定位偏差。
- **修复**：
  - `transcribe_words` 滤除纯标点 token；
  - `find_occurrences` 在**去标点连接文本**上匹配，字符位置经 `idx` 映射回词级时间戳，
    跨标点命中（如「大家好，今天」查「好今天」）且时间精确到字级。

### [2026-09-01] 健壮性 | 标点模型加载失败导致整个识别不可用

- **修复**：`get_asr()` 先带 `punc_model` 加载，异常时自动去掉标点模型重试一次（无标点降级）。

### [2026-09-01] 边界 | 分割段碎片化

- **问题**：留白过大或匹配过密时产生 <50ms 碎段、甚至起止倒挂。
- **修复**：`compute_split_segments` 用游标推进并 `max(cur, …)` 防回退，过滤 <50ms 段；
  结果为空时给用户明确提示（「留白设置可能过大」）。

### [2026-09-01] 输入校验 | 非法正则导致后台任务崩溃

- **修复**：正则模式下提交前 `re.compile` 预检，失败弹窗提示，不进入任务线程。

---

## 2026-09-01 去背景音乐（人声/伴奏分离）

### [2026-09-01] 选型 | 分离模型来源

- **背景**：需专用人声/伴奏分离模型。魔搭有 `DiffSynth-Studio/Demucs-Repackage`（facebook Demucs 魔搭转存）。
- **决策**：采用 **torchaudio 内置 `HDEMUCS_HIGH_MUSDB_PLUS`**（MUSDB18-HQ 高质量权重，44.1k 立体声、4 音轨）。
  原因：零新增 pip 依赖（demucs 包 / DiffSynth-Studio 均需引入额外依赖树）；
  权重已预下载缓存至 `~/.cache/torch/hub/torchaudio/models/hdemucs_high_trained.pt`（319MB），此后离线。
  魔搭 Demucs-Repackage 权重仍可通过「设置→分离模型目录」接入（放置转换后的权重文件）。

### [2026-09-01] 兼容 | torchaudio 2.11 load/save 需要 torchcodec

- **问题**：`torchaudio.load/save` 报 `ImportError: TorchCodec is required`。
  （torchaudio 2.9 起媒体 IO 后端切换为 torchcodec，venv 中未安装。）
- **修复**：`separation.separate` 改用 **soundfile** 读写 WAV（项目已有依赖），
  仅保留 `torchaudio.functional.resample`（纯张量运算，无后端依赖）。
  该问题由端到端验证暴露（冒烟测试第 10 项只做模型 forward，未覆盖 IO）。

### [2026-09-01] 新增 | 去背景音乐功能

- 新模块 `app/separation.py`：HDemucs 分块推理（10s 窗 / 75% 跳步 / 线性交叠加权融合），
  长音频内存恒定；输出 = 人声 + 可调伴奏保留比例；可选同时输出伴奏文件。
- 界面：「消除杂音」页新增「去背景音乐」分组；「设置」页新增「分离模型目录」。
- 验证：冒烟测试 11 项全过；端到端（合成人声+和弦音频 → 分离 → mp3 编码）通过，
  6 秒音频 CPU 耗时约 10 秒。

### [2026-09-01] 测试 | 冒烟测试扩至 11 项

- 第 10 项：分离模块（模型加载 + 2 秒前向，断言 4 音轨输出形状）；
  权重不可用环境下降级为 skip，不阻塞其余测试。GUI 构造顺延为第 11 项。

---

## 2026-09-02 音频翻译与文字转语音

### [2026-09-02] 新增 | 音频翻译（英译中 / 中译英 / 日译中）

- 新模块 `app/translator.py`：语音识别（句级时间戳）→ 逐句翻译 → 双语导出
  （`<源>.翻译.txt` 双语对照 / `<源>.双语.srt` 双语字幕 / `<源>.双语.lrc` 双语歌词）。
- 双后端：本地 M2M100（`facebook/m2m100_418M`，HF 镜像自动下载或设置页指定权重目录，完全离线）/
  OpenAI 兼容 chat API（`api_base` 指向本地 Ollama 或线上服务均可）。
- 双语 SRT/LRC 导出复用 `asr._fmt_srt` / `asr._fmt_lrc`，时间轴与识别结果一致。

### [2026-09-02] 新增 | 文字转语音（中 / 英 / 粤）

- 新模块 `app/tts.py`：6 个音色（中文普通话 x2、英语 x2、粤语 x2），输出 MP3，
  留空路径自动命名到 `tts_out\`。
- 双后端：edge-tts（微软线上免费，需联网）/ OpenAI 兼容 `/v1/audio/speech` 接口。

### [2026-09-02] 变更 | 依赖与配置扩展

- `pyproject.toml`：modelscope extra 增加 `transformers`、`edge-tts`；base 增加 `requests`。
- `config.py` DEFAULTS 新增 `translate_backend`、`translate_model_dir`、`tts_backend`、
  `tts_api_model`、`api_base`、`api_key`、`api_model`。
- 界面：新增「翻译 / TTS」页签；「设置」页新增「翻译 / TTS 服务」分组（后端切换、
  API 参数、M2M100 权重目录）。

### [2026-09-02] 测试 | 冒烟测试扩至 12 项

- 第 11 项：翻译/TTS 模块断言（语言对、音色表）+ 双语 TXT/SRT/LRC 导出内容校验（纯逻辑离线）；
  GUI 构造顺延为第 12 项。2026-09-02 全部通过。

---

## 2026-09-02 播放修复与界面精简

### [2026-09-02] 界面 | 视频播放无图像（有声音）

- **问题**：打开视频文件播放，声音正常但画面区域黑屏。
- **原因**：`QVideoWidget` 已创建并加入布局，但从未调用
  `QMediaPlayer.setVideoOutput()` 绑定解码画面，播放器视频流无处渲染。
- **修复**：`MainWindow.__init__` 中在 `_build_ui()` 之后补上
  `self.player.setVideoOutput(self.video)`。

### [2026-09-02] 界面 | 移除「慢速 0.5x」按钮

- **问题**：倍速下拉已含 0.5x 档位，独立慢速按钮功能重复。
- **修复**：删除 `btn_slow` 控件、`_slow_toggle` 方法及信号连接与联动逻辑；
  慢速直接在倍速下拉选择 0.5x。文档同步更新。

### [2026-09-02] 功能 | 时间轴页签新增音画同步微调

- **需求**：视频音频不同步时，可微调音频时间轴（提前/推迟播放）使音画同步，
  播放时可试听校准，确认后可导出新文件。
- **实现**：
  - `ffmpeg_tools`：`_sync_audio_args`（正偏移 adelay 补静音 / 负偏移 atrim 掐头等价前移）、
    `av_sync_offset`（全片应用，视频 copy + 音频 aac）、`av_sync_preview`
    （当前位置起 20 秒校准片段，视频音频均重编码保证对齐准确）。
  - 时间轴页签新增「音画同步微调」分组：偏移量 ±10 秒（步进 0.05）、
    试听预览（自动播放校准片段）、恢复播放原文件、应用到文件。
  - 边界处理：`-shortest` 仅用于正偏移（负偏移若加会截掉视频尾部，实测 3.0s→2.52s）；
    纯音频文件禁用（无画面无同步意义）。
- **验证**：端到端 ffmpeg 实测（3 秒测试视频 ±0.5s 偏移，双向均保持视频全长 3.0s）。

### [2026-09-02] 功能 | 波形增强：时间刻度 + 立体声双行 + 缩放；帧步进

- **需求**：波形提供时间刻度与立体声 L/R 两行显示；支持缩放查看；
  评估并增加视频帧进度能力辅助音画对齐。
- **实现**：
  - `WaveformWidget` 重写：底部时间刻度尺（步进随缩放自适应 0.01s~1h）；
    立体声 L/R 双行（单声道单行）；视图缩放（滚轮水平滚动、Ctrl+滚轮以光标
    为中心缩放、播放中播放头越界自动跟随、视图外边缘红色三角指示）。
  - 缩放深度：≤10 分钟文件保留原始采样可放大到采样级；更长文件受概览
    包络分辨率（最多 12 万桶）限制，缩放下限自动计算。
  - `audio_ops.load_wav` 增加 `mono` 参数（波形改用立体声加载）。
  - 帧步进：`MediaInfo.fps`（ffprobe avg_frame_rate 解析）+「« 帧 / 帧 »」
    按钮与帧号显示，按帧前进/后退辅助音画对齐（评估结论：轻量实现即可，
    不做帧缩略图条）。
- **验证**：smoke_test 第 12 项新增断言（双行包络、缩放窗口、端点映射、
  滚动钳制、播放头跟随、刻度步进取整、fps 解析），12/12 全过。

### [2026-09-03] 功能 | 分离音频支持多格式并保留原始音频参数

- **问题**：合并/分离页签的分离功能只能输出 WAV，且「16kHz 单声道」选项
  对用户分离无意义（该参数本是内部 DSP/ASR 提取用）。
- **修复**：
  - `ffmpeg_tools` 新增 `AUDIO_OUT_FORMATS` 格式表与 `extract_audio_keep()`：
    不指定 `-ar/-ac`，保留源文件原始采样率/声道，按扩展名编码
    （WAV 无损 / FLAC 无损 / MP3 VBR / M4A·AAC 192k / OGG Vorbis / OPUS 128k）；
    内部 DSP 用的 `extract_audio`（16k 单声道）保持不变。
  - 界面：分离分组改为「输出格式」下拉 + 分离按钮，移除 16kHz 单声道复选框。
  - 注：OPUS 编解码标准固定 48kHz（libopus 自动重采样），其余格式实测
    44.1kHz 立体声源精确保留。
- **验证**：端到端 ffmpeg 实测 6 种格式（声道均保留立体声；除 OPUS 按标准
  48kHz 外采样率均 44100Hz）；smoke_test 第 7 项增加格式表与非法格式断言。

### [2026-09-03] 功能 | 新增「格式转换」页签（批量视频转音频/音频转音频）

- **需求**：单个或多个媒体文件批量格式转换（视频转音频、音频转音频），
  可自定义码率、采样率、降噪率，默认跟随源文件码率与采样率。
- **实现**：
  - `ffmpeg_tools`：新增 `convert_media(src, dst, bitrate, sr, denoise)`
    与 `_audio_codec_args(ext, bitrate)`（码率覆盖默认，无损格式忽略码率）；
    `AUDIO_OUT_FORMATS` 简化为扩展名→说明表，`extract_audio_keep` 复用
    统一编码参数。降噪路径：提取 wav（保留声道）→ noisereduce 稳态谱减
    （立体声按 (channels, samples) 逐声道处理）→ 再编码，临时文件自动清理。
  - 界面：新增「格式转换」页签（合并/分离之后）——文件列表（多选添加/
    移除/清空）、输出格式下拉、码率可编辑下拉（源/默认、128k~320k 或手填）、
    采样率下拉（源 / 8k~48k）、可选降噪强度 0.1~1.0；
    输出到源同目录 `<源名>.<格式>`，同格式转码自动加 `_转换` 后缀防覆盖。
- **验证**：端到端实测 44.1kHz 立体声视频 → mp3(128k/22050Hz 生效)、
  m4a(默认跟随源 44100)、降噪 wav（声道/采样率保留）；非法格式报错；
  smoke_test 第 7 项增加转换断言，12/12 全过。

### [2026-09-03] 功能 | 波形底部概览条（比例条）支持鼠标拖拽缩放

- **需求**：除滚轮/Ctrl+滚轮外，波形底部的比例条也应能用鼠标左右拖拽进行缩放。
- **实现**（WaveformWidget）：
  - 波形区下方新增 42px **概览条**：全长缩略波形（每像素 min/max 包络）+
    当前视图窗口（黄色框 + 左右黄色手柄）+ 窗口外压暗 + 概览播放头。
  - 鼠标交互（与波形区点击/选区互斥，按 y 坐标分流）：
    拖窗口内部 = 平移视图；拖左/右边缘 = 以对侧为锚缩放；
    窗口外拖拽框选 = 放大到该区间（小于 4 像素视为误触）；
    双击概览条 = 复位全览。
  - hover 光标随位置切换（OpenHand/ClosedHand/SplitH/Cross）；
    新增 `_set_view` 统一钳制视图（修复越界时窗口跨度可超过总时长的 bug）、
    `reset_view`、`_ov_hit` 命中测试。
  - 波形最小高度 170→210（容纳概览条）。
- **验证**：smoke_test 第 12 项新增概览条断言（命中测试四类区域、
  拖边缘锚定、越界钳制、最小窗口、框选放大、双击复位），12/12 全过。

---

## 2026-09-03 目录批处理与文件名/夹处理

### [2026-09-03] 新增 | 文件/目录批处理工具模块 `app/filetools.py`

- **需求**：格式转换源选择支持文件与目录混合，目录需递归穷举子目录及文件；
  输出到目标目录时保持源相对目录结构；转换与分割均可选择是否限制文件名长度。
- **实现**（新模块 `app/filetools.py`）：
  - `scan_inputs(paths, exts, recursive)`：文件+目录混合输入，目录 `os.walk` 递归、
    按扩展名集合过滤、去重排序；非递归只取目录直属文件。
  - `source_root(paths)`：取输入中的第一个目录为结构根；全文件输入取公共父目录。
  - `plan_output(src, root, out_root, ext, keep_structure, max_name_len)`：
    用 `relpath` 在输出根下重建相对子目录；输出根留空回源目录；
    主干超长按字符截断（保留扩展名）；目标重名自动加 `_1/_2` 序号（`unique_path`）。
  - `truncate_stem(stem, max_len)`：按字符数截断（中文按字）。
- **格式转换页签**：新增「添加目录…」按钮、递归子目录勾选（默认开）、
  输出根目录浏览、保持相对目录结构勾选（默认开）、文件名长度勾选+字符数 SpinBox；
  批处理任务改用 `scan_inputs` 收集、`plan_output` 定位输出。
- **分割页签**：新增「分割输出选项」分组——「为每个源文件创建子目录」默认勾选
  （`<源目录>\<源名>_<类型>分割\`，分段名不带前缀）；不勾选时分段直接放源目录、
  文件名加 `<源名>_` 前缀（定长 `part_`、特征 `piece_`、关键词 `seg_`）；
  文件名长度勾选同时作用于子目录名与前缀。

### [2026-09-03] 新增 | 「文件名/夹处理」页签（批量重命名）

- **需求**：新增页签用于批量对齐文件名长度、文件名替换，功能对齐
  `C:\Windows\System32\xrename.bat`（by pjt）。
- **实现**（`filetools.transform_stem` / `plan_rename` / `apply_rename`）：
  四种模式映射 Python 切片，位置为 0 基字符索引、支持负数：
  - 字符串替换 `replace`：`stem.replace(find, repl)`（替换串留空=删除）；
  - 保留子串 `keep`：从第 n 位保留 m 位（m<0 → `stem[n:m]` 保留到倒数 |m| 位）；
  - 左切除 `lcut`：`stem[m:]` 去掉前 m 位；
  - 中间切除 `cut`：`stem[:n]+stem[m:]` 删除第 n~m 位之间的字符。
  - 安全保护（沿用 xrename.bat 语义）：结果为空跳过、新旧名相同跳过、
    目标已存在不覆盖（可勾选自动加序号）；超长截断可选。
  - 界面：源目录+递归子目录+扩展名过滤（逗号分隔，空=全部）、模式下拉、
    n/m SpinBox、查找/替换串、重名自动序号勾选、长度勾选、
    「预览」只读对照框（旧→新/跳过原因+统计）、「执行」前弹确认框，后台线程执行。
- **验证**：smoke_test 新增第 13 项（scan 递归/非递归+扩展名过滤、
  plan_output 结构保持/平铺/截断/重名序号、transform_stem 四模式含负数 m、
  plan_rename 未变化/空名保护、apply_rename 实际落盘），**13/13 全过**。

---

## 回归基线

`smoke_test.py` 当前 13 项（2026-09-03 全过）：

1. 依赖导入 → 2. 模块导入 → 3. 合成测试音频 → 4. 特征检测（哔哔/削波/静音）
   → 5. 消除管线 → 6. 说话人匹配 → 7. ffmpeg 工具 → 8. ASR 导出格式（TXT/SRT/LRC）
   → 9. 关键词/正则分割计算（三种策略断言 + 跨标点命中）→ 10. 人声/伴奏分离（HDemucs 前向）
   → 11. 翻译/TTS 模块与双语导出 → 12. GUI offscreen 构造
   → 13. 文件/目录批处理（递归扫描/结构保持/截断/重命名四模式）。
